services:
- docker
install: make build
stages:
- name: echo
- name: publish
  if: branch = main AND type != pull_request
jobs:
  include:
  - stage: echo
    script: echo
  - stage: publish
    name: Publish production image
    script:
    - make publish
env:
  global:
